var main = function(){
    
    TESTER = $('#tester');
    
    //Plotly.plot( TESTER, [{x: [1, 2, 3, 4, 5],y: [1, 2, 4, 8, 16] }], {margin: { t: 0 } });
  
    console.log(Plotly.BUILD);
}

$(document).ready(main)